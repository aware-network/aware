# Aware Meta Graph Panes

Module-owned pane package for General Meta graph views.

The first pane mounts `aware.meta.graph.canvas` through authored PaneRenderSpec
truth over the `aware_meta.graph.canvas.v1` view-state contract. It does not use
the old Flutter graph viewer rail directly.
