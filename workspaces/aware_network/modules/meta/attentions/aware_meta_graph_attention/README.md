# Aware Meta Graph Attention

Module-owned Attention layout topology for the General Meta graph canvas.

The package declares the `graph_canvas` layout and `graph` section consumed by
`aware_meta_experience/profiles.aware`. Pane rendering stays in
`interfaces/panes/aware_meta_graph_panes`; this package only owns layout section
truth.
