# aware-storage-sdk

Handwritten Storage SDK facade over the generated Storage service API.

The SDK resolves media through Storage API descriptors. It does not construct
Node raw download URLs and does not import Storage service internals.
