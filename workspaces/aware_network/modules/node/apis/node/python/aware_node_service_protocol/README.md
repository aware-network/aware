# aware_node_service_protocol

Public generated API client and service protocol boundary for live Node host provisioning, environment config, and service route discovery authority.

## Installation

```bash
pip install .
```
