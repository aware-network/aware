# Node Session Status Pane

Canonical pane package for local or remote node/session readiness. It keeps
renderer-visible status in the Interface package instead of adding a separate
CLI-only node rail.
