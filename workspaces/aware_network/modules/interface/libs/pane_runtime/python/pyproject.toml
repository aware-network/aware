[project]
name = "aware-interface-pane-runtime"
version = "0.1.0"
description = "Interface-owned pane and lane materialization runtime contracts."
authors = [{ name = "AWARE Team" }]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aware_interface_pane_runtime"]

