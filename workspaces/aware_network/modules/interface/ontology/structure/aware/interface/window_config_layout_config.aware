class WindowConfigLayoutConfig {
    // Relationships
    layout_config aware_attention.layout.LayoutConfig key

    // Attributes
    description String?
    is_default Bool = false

    fn build construct (
        layout_config_id UUID key,
        description String? = null,
        is_default Bool = false
    ) -> WindowConfigLayoutConfig {
        """
        Create one deterministic WindowConfig↔LayoutConfig bridge.
        """
    }

    fn set_attachment_config (
        description String? = null,
        is_default Bool = false
    ) -> WindowConfigLayoutConfig {
        """
        Update the WindowConfig-scoped layout attachment on the join itself.
        """
    }
}

ann interface.WindowConfigLayoutConfig::layout_config load eager
