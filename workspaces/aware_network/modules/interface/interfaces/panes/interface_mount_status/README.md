# Interface Mount Status Pane

Canonical pane package for the active Interface package mount state. The first
version is descriptor-only; host/runtime actions must still be introduced
through mounted endpoint contracts, not local shortcuts.
