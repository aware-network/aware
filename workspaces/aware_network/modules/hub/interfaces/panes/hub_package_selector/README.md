# Hub Package Selector Pane

Canonical pane package for Hub-backed package discovery and artifact selection.
It is the first mounted-action proof for the control rail: renderers emit pane
actions, and the Interface host routes them to Hub API endpoints.
