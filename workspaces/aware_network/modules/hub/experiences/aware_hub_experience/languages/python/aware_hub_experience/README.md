# aware-hub-experience

Public Hub view-state package for Aware discovery surfaces.

This package contains typed Python models for Hub discovery and package map
views. It is a view contract package only; Hub authority remains behind the
generated Hub API client, Hub SDK, and Hub service runtime.
