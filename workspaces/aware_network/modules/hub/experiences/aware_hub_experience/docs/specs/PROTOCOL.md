# Aware Hub Specs — Local Protocol

This local spec surface follows the canonical repo protocol:

- `docs/specs/PROTOCOL.md`

Local lock for `aware-hub`:

1. Hub is an environment experience, not a custom interface product rail.
2. Interface movement must stay on commit-driven attention/focus materialization rails.
3. No custom model contracts and no hub-only endpoint strategy.

Execution rules:

- Every implementation iteration maps to exactly one issue under `docs/issues/**`.
- Iteration evidence is recorded in:
  - issue `Updates`
  - spec `PHASES.md` phase ledger
  - daily feed `docs/feed/YYYY/MM/DD.md`
- Canonical mutation rail remains `aware-cli commit` (outside this planning-only setup lane).
