# Aware Hub — Specs

Spec packages for this workspace follow:

- `docs/specs/PROTOCOL.md` (repo root canonical protocol)
- `experiences/aware-hub/docs/specs/PROTOCOL.md` (local execution lock)

## Spec Packages

- `hub-environment-experience/` (entrypoint: `hub-environment-experience/README.md`)
- `hub-attention-layout/` (entrypoint: `hub-attention-layout/README.md`)

Notes:

- This surface is contract-first.
- Implementation issues must map to one approved iteration artifact under one of these packages.
