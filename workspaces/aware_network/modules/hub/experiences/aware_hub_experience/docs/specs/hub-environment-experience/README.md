# Spec — Hub Environment Experience

This spec package locks how Hub is defined as an `EnvironmentExperience`.

Entrypoints:

- Contract: `SPEC.md`
- Roadmap + evidence ledger: `PHASES.md`
- Iteration contract: `iterations/PROTOCOL.md`

Execution rule:

- Every implementation iteration must map to exactly one issue and one approved iteration artifact.
