# Spec — Hub Attention Layout

This spec package locks how Hub consumes canonical attention layout rails.

Entrypoints:

- Contract: `SPEC.md`
- Roadmap + evidence ledger: `PHASES.md`
- Iteration contract: `iterations/PROTOCOL.md`

Execution rule:

- Hub attention layout work must remain consumer-only relative to attention compiler ownership.
