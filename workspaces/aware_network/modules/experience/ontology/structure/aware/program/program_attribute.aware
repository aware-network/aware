class ProgramAttribute {
    // Relationships
    config aware_experience.program.ProgramConfigAttributeConfig key
    attribute aware_meta.attribute.Attribute key

    fn build construct (program_id UUID, config_id UUID, attribute_id UUID) -> ProgramAttribute {
        """
        Create deterministic attribute association edge for one Program.
        """
    }
}
