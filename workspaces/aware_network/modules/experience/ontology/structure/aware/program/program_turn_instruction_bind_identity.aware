/// Canonical node-alias resolution receipt under one ProgramTurnInstructionBind.
///
/// Contract:
/// - Freezes bind-time resolution of one ProgramConfig port-node contract to one ClassInstanceIdentity.
/// - References graph wiring bridge used for the resolution proof.
class ProgramTurnInstructionBindIdentity {
    // Relationships
    program_config_port_projection_experience_node aware_experience.program.ProgramConfigPortProjectionExperienceNode key
    projection_experience_node_class_identity aware_experience.projection.ProjectionExperienceNodeClassIdentity key

    fn build construct (
        program_config_port_projection_experience_node_id UUID key,
        projection_experience_node_class_identity_id UUID key
    ) -> ProgramTurnInstructionBindIdentity {
        """
        Create deterministic ProgramTurnInstructionBindIdentity under ProgramTurnInstructionBind.
        """
    }
}
