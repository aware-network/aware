# Aware Network Experience

Canonical Network territory discovery experience for Control ingress.

This package owns `aware_network.territory.discovery.v1`, a view-state contract for the public node/environment/service territory plane. Runtime state is produced by the Network SDK provider over Network Service API truth.
