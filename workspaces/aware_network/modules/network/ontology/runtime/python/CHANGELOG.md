# Aware Network Changelog

## 2025-10-15
- Added interface session binding manager and websocket control-frame enforcement to build the registry needed for commit fan-out.
- Introduced documentation and unit tests for the binding lifecycle (`interface_session_binding_manager`).
