[project]
name = "aware-network-ontology"
version = "0.1.0"
description = "Auto-generated package aware-network-ontology"
authors = [{ name = "Aware Automations" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aware-orm"
]

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aware_network_ontology"]
include = ["README.md", "aware_network_ontology/py.typed"]
