[project]
name = "aware-attention-sdk"
version = "0.1.0"
description = "Attention SDK facade over the generated Attention service API."
authors = [{ name = "AWARE Team" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "aware_attention_service_api",
]

[project.optional-dependencies]
test = [
  "aware-sdk-network",
  "pytest>=8.4.2",
  "pytest-asyncio>=0.23.7",
]

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aware_attention_sdk"]
include = ["README.md", "aware_attention_sdk/py.typed"]
