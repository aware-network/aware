[project]
name = "aware_skill_service_protocol"
version = "0.1.0"
description = "Public Product A/Product B API boundary for invoking Skill runs through Service."
authors = [{ name = "Aware Automations" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aware_skill_service_dto",
    "aware-utils",
    "pydantic>=2.13.4,<3.0.0"
]

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aware_skill_service_protocol"]
include = ["README.md", "aware_skill_service_protocol/py.typed"]
