# aware_identity_service_protocol

Public Identity service API boundary for canonical Identity admission, credential profiles, and actor-role assignment over the class-instance-aware role rail.

## Installation

```bash
pip install .
```
