enum CredentialTargetKind {
    aware_api
    aware_service
    test_pypi
    pypi
    github
    oci
    hub
    generic
}

enum CredentialKind {
    api_key
    bearer_token
    oauth2_access_token
    oidc_trusted_publisher
    ssh_key
    certificate
    password
    generic_secret
}

enum CredentialSecretResolverKind {
    env_var
    pypirc
    local_file
    aware_secrets_dir
    ci_secret
    external_vault
    trusted_publisher_oidc
    no_secret
}

enum CredentialProfileStatus {
    planned
    ready
    blocked
    revoked
    expired
}

enum CredentialGrantEffect {
    allow
    deny
}

enum CredentialReadinessStatus {
    unknown
    ready
    missing
    invalid
    unauthorized
    blocked
}

enum CredentialUsageStatus {
    planned
    succeeded
    failed
    blocked
    skipped
}
