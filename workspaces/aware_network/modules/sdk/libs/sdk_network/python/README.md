# Aware SDK Network

Network transport adapters and live-test fixtures for public Aware SDK packages.

This package owns provider-ref route loading, remote Node API endpoint clients,
and websocket-backed transport helpers. The kernel-owned `aware-sdk-core`
package stays product-neutral and transport-free.
