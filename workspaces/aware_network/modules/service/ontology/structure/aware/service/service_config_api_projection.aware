class ServiceConfigApiProjection {
    // Relationships
    api_graph_projection aware_api.api.ApiGraphProjection key

    // Attributes
    description String?

    fn build construct (
        api_graph_projection_id UUID key,
        description String? = null
    ) -> ServiceConfigApiProjection {
        """
        Create one config-level bridge from a ServiceConfigApi to one API-owned graph projection.
        """
    }
}
