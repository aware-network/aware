name: aware_api
description: High-level Dart client for interacting with AWARE nodes via DTOs.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.2
  http_parser: ^4.0.2
  logging: ^1.2.0
  uuid: ^4.5.1

dev_dependencies:
  test: ^1.25.8
