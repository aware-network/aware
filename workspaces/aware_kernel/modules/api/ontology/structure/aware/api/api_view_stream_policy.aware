/// Optional stream policy for one readable API view contract.
class ApiViewStreamPolicy {
    // Attributes
    stream_mode ApiViewStreamMode key
    description String?

    fn build construct (
        stream_mode ApiViewStreamMode key,
        description String? = null
    ) -> ApiViewStreamPolicy {
        """
        Create one API view stream policy beneath ApiView.
        """
    }
}
