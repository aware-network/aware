[project]
name = "aware-storage-ontology"
version = "0.1.0"
description = "Auto-generated package aware-storage-ontology"
authors = [{ name = "Aware Automations" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aware_storage_ontology"]
include = ["README.md", "aware_storage_ontology/py.typed"]
