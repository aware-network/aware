[project]
name = "aware_storage_service_api"
version = "0.1.0"
description = "Public API boundary for commit-backed StorageBlob metadata and Storage-owned media resolution."
authors = [{ name = "Aware Automations" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aware_storage_service_dto",
    "aware-api-client",
    "aware-types",
    "aware-utils",
    "pydantic>=2.8.0,<3.0.0"
]

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aware_storage_service_api"]
include = ["README.md", "aware_storage_service_api/py.typed"]
