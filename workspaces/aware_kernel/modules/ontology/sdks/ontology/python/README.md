# aware-ontology-sdk

Consumer-facing Ontology SDK over the generated Ontology Product A API client.

This package may import `aware_ontology_service_api` and
`aware_ontology_service_dto`. It must not import `aware_ontology_service_protocol`
or `services/ontology` internals.
