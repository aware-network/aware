[project]
name = "aware-ontology-ontology-orm-models"
version = "0.1.0"
description = "Auto-generated package aware-ontology-ontology-orm-models"
authors = [{ name = "Aware Automations" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aware_ontology_ontology_orm_models"]
include = ["README.md", "aware_ontology_ontology_orm_models/py.typed"]
