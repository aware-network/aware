[project]
name = "aware-reactivity-ontology-orm-models"
version = "0.1.0"
description = "Auto-generated package aware-reactivity-ontology-orm-models"
authors = [{ name = "Aware Automations" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aware_reactivity_ontology_orm_models"]
include = ["README.md", "aware_reactivity_ontology_orm_models/py.typed"]
