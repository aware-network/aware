[project]
name = "aware-history-ontology-dto"
version = "0.1.0"
description = "Auto-generated package aware-history-ontology-dto"
authors = [{ name = "Aware Automations" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aware_history_ontology_dto"]
include = ["README.md", "aware_history_ontology_dto/py.typed"]
